package ch13.work.facade;

public class ShapeMaker {
    /*private SquareAdapter squareAdapter;
    private CircleAdapter circleAdapter;
    private RectangleAdapter rectangleAdapter;
    private LineAdapter lineAdapter;

    public ShapeMaker() {
        circleAdapter = new CircleAdapter();
        rectangle = new Rectangle();
        square = new Square();
    }

    public void drawLine(int x1, int x2, int y1, int y2) { lineAdapter.draw(x1, x2, y1, y2); }
    public void drawCircle(int x1, int x2, int y1, int y2){
        circleAdapter.draw(x1, x2, y1, y2);
    }
    public void drawRectangle(int x1, int x2, int y1, int y2){
        rectangleAdapter.draw(x1, x2, y1, y2);
    }
    public void drawSquare(int x1, int x2, int y1, int y2){
        squareAdapter.draw(x1, x2, y1, y2);
    }*/
}
